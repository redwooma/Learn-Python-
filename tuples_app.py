# tuples a data structure
# container to contain different values
# similar to a list : stores multiple pieces of information

# coordinates are the most common tuples
# immutable
coordinates = (4, 5)
# coordinates[1] = 10 # errors
print(coordinates[0])

coordinates = [(4, 5), (6, 7), (80, 34)]
coordinates[1] = 10
print(coordinates[1])
